import { TotalStudentsPipe } from './total-students-pipe';

describe('TotalStudentsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalStudentsPipe();
    expect(pipe).toBeTruthy();
  });
});
